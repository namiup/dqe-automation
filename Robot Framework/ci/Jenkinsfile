pipeline {
    agent any
    stages {
        stage('Update Packages') {
            steps {
                script {
                    sh '''
                        # Update package lists and install dependencies
                        apt-get update
                        apt-get install -y curl gnupg wget

                        # Add Google Chrome repository
                        wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | gpg --dearmor > /usr/share/keyrings/google-chrome.gpg
                        echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google-chrome.gpg] http://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources.list.d/google-chrome.list

                        # Update package list again to include the Chrome repository
                        apt-get update

                        # Force install the latest version of Google Chrome
                        apt-get install -y google-chrome-stable

                        # Verify the installed version of Google Chrome
                        google-chrome --version
                    '''
                }
            }
        }
    }
    post {
        always {
            echo 'Pipeline execution completed.'
        }
        success {
            echo 'Google Chrome updated successfully!'
        }
        failure {
            echo 'Failed to update Google Chrome. Please check the logs for details.'
        }
    }
}
